# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables (Firebase config, etc.)
.env
.env.*
!.env.example

# Firebase
.firebase
firebase-debug.log
firebase-debug.*.log
ui-debug.log

# Personal documents dropped in for testing — payslips, bank statements,
# receipts. These carry your name, employee number, employer and salary, and
# git never forgets: once one is committed it stays in the history even after
# the file is deleted.
*.pdf
*statement*
*payslip*
*明細*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
